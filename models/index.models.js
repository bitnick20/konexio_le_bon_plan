const User = require('./user.models');
const Picture = require('./picture.models');
const Message = require('./message.models');
const City = require('./city.models');
const Announcement = require('./announcement.models');

module.exports = {
    User: require('./user.models')
}

module.exports = {
    Picture: require('./picture.models')
}

module.exports = {
    Message: require('./message.models')
}

module.exports = {
    City: require('./city.models')
}

module.exports = {
    Announcement: require('./announcement.models')
}